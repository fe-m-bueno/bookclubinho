legacy-peer-deps=true

# Supply chain: não resolve versões publicadas há menos de 7 dias, janela em que a
# maioria dos pacotes comprometidos é detectada e despublicada.
# Requer npm >= 11.10 para ter efeito; versões anteriores ignoram a chave.
min-release-age=7
